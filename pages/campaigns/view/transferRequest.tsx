import React from 'react'
import RequestList from '../../../src/modules/campaign/screens/RequestList'

function transferRequest() {
  return (
    <RequestList />
  )
}

export default React.memo(transferRequest)